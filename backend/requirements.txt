# ── Core (required) ──────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
motor>=3.3.0
pymongo>=4.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0
aiohttp>=3.9.0
aiosmtplib>=3.0.0
jinja2>=3.1.2
fpdf2>=2.7.0
httpx>=0.25.0
python-dotenv>=1.0.0
bcrypt>=4.1.0
google-genai>=1.0.0

# ── AI / ML (optional — gracefully degrade if missing) ──
# sentence-transformers needs PyTorch (~800MB) — omit for lightweight deploys
# deepface needs TensorFlow (~1.5GB) — omit for lightweight deploys
# Install locally: pip install sentence-transformers deepface
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# ── Document parsing ──────────────────────────
PyPDF2>=3.0.0
python-docx>=1.0.0
opencv-python-headless>=4.8.0
